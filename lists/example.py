supplies = ['pens', 'staplers', 'flame-throwers', 'binders']

for i in range(len(supplies)): # ising range, len functions to work on an list callied supplies
   print("index " + str(i) + " in supplies is " + supplies[i])